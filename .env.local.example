# UncannyPrompt local overrides (.env.local)
#
# Purpose: override .env values for dotnet run / IDE scenarios on the host.
# Keep only values that differ from the Docker-network defaults in .env.

# --- Database host overrides for local tooling ---
Database__Host=localhost
Database__Port=14331
